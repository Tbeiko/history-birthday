%section.container
  .row
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_tag url: '/', html: { class: "form form-inline" } do |f|
        %header
          %h1 When is your birthday?
        %fieldset
          = f.text_field :day,   label: "Day"
          = f.text_field :month, label: "Month"
          = f.text_field :year,  label: "Year"
          %button.btn.btn-default(type="submit") Submit